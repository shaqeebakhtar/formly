import React from 'react';

type Props = {};

const ResponsesTable = (props: Props) => {
  return (
    <div className="w-full rounded-lg bg-card p-6 mt-6 shadow">
      ResponsesTable
    </div>
  );
};

export default ResponsesTable;
